<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Client.Project.TestAdd"  
        xmlns="http://schemas.microsoft.com/dotnet/2021/maui"    
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
        Title="Вопросы">

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="Center" Margin="10">
                <Button Text="Сохранить" Clicked="Button_Clicked" />
                <Button Text="Отмена" Clicked="Button_Clicked_1" />
                <Button Text="Добавить вопрос" Command="{Binding AddQuestionCommand}" />
            </StackLayout>

            <ListView Grid.Row="2" ItemsSource="{Binding QuestionsList}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding QuestionName}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>